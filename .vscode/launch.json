{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Compile and Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/debug/main.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "preLaunchTask": "Build Debug Project"
        }
    ]
}